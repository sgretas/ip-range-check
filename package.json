{
  "name": "ip-range-check",
  "version": "1.0.0",
  "description": "This app is using the npm package ip-range-check to check if a list of IP addresses is included in a list of IP ranges. It returns only the IP addresses that are out of bounds.",
  "main": "src/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./src/app.js"
  },
  "author": "Simon Gretas",
  "license": "ISC",
  "dependencies": {
    "fs": "^0.0.1-security",
    "ip-range-check": "^0.2.0"
  }
}
